// Using a while loop, print out the numbers between 1 and 10 inclusive, one on a line. Example output:
// prompt the user for the number to start on and the number to end on.

// let start = prompt(`What number to start on?`)
start = 0
//let end = prompt(`What number to end on?`)
end = 6

for (let x = start; x <= end; x++) {
    console.log(x)
}